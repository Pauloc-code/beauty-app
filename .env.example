# Configuração do Banco de Dados
DATABASE_URL="postgresql://usuario:senha@localhost:5432/beauty_app"

# Configurações do PostgreSQL (se usando localmente)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=sua_senha
PGDATABASE=beauty_app

# Ambiente
NODE_ENV=development

# Porta do servidor (opcional - padrão: 5000)
PORT=5000